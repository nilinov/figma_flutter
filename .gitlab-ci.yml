stages:
  - staging

deploy_staging:
  stage: staging
  script:
    - apt-get update -qq
    - apt-get install -qq git
    # Setup SSH deploy keys
    - 'which ssh-agent || ( apt-get install -qq openssh-client )'
    - eval $(ssh-agent -s)
    - ssh-add <(echo "$SSH_PRIVATE_KEY")
    - ssh ovz2.j1121565.noklm.vps.myjino.ru -p 49398 -l flutter "cd /home/flutter/iframe &&
        ./update.sh "
  environment: staging
  only:
    - main
